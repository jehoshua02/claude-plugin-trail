# Claude Instructions

## Version Bumps

Always bump the version in `.claude-plugin/plugin.json` when making functional changes to the plugin. Do it in the same commit as the code changes, not as a separate afterthought.
